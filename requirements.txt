# --- LiveKit Agents core + telephony + SIP API ---
# NOTE (Rishi): these are current 1.x-line pins. After creating a venv and
# running `pip install -r requirements.txt`, verify the exact plugin/event/method
# names used in src/agent.py and src/dispatcher.py against the installed version.
livekit-agents[deepgram,cartesia,openai,silero,turn-detector]~=1.0
livekit-api~=1.0          # server API: create rooms + outbound SIP participants

# --- Direct OpenAI SDK (used by reasoner.py for the follow-up decision) ---
# LLM is GPT-4o for now. TODO(Rishi): swap the model if we change providers later.
openai>=1.40.0

# --- Config / data ---
python-dotenv>=1.0.1
PyYAML>=6.0.2

# --- Tests ---
pytest>=8.0.0
