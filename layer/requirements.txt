# Backend for the compliance + intelligence layer / dashboard.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
PyYAML>=6.0.2

# Optional: enables the Claude-based interpreter (falls back to heuristic if absent).
anthropic>=0.39.0
