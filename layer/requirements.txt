# Backend for the compliance + intelligence layer / dashboard.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
PyYAML>=6.0.2

# Optional: for the OpenAI GPT-4o interpreter backend (once wired). The layer
# runs on the heuristic interpreter without it.
openai>=1.40.0
